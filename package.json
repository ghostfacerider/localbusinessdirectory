{
  "name": "project",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "api": "nodemon cd backend && ./bin/www",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run api\" \" npm run client\" --kill-others --kill-others-on-fail "
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "nodemon": "^3.1.0"
  }
}
